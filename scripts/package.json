{
  "name": "tunnel-non-zk-scripts",
  "version": "1.0.0",
  "description": "TypeScript utilities and testing for non-ZK tunnel",
  "type": "module",
  "scripts": {
    "deploy": "tsx src/deploy.ts",
    "test": "tsx src/test.ts",
    "verify-deletion": "tsx src/verify-deletion.ts",
    "audit": "tsx src/audit.ts",
    "test:multiple-claims": "tsx src/test-multiple-claims.ts",
    "test:all": "npm run test && npm run verify-deletion && npm run audit"
  },
  "dependencies": {
    "@mysten/sui": "^1.15.0",
    "@noble/ed25519": "^2.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
